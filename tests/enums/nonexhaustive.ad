# A `match` that omits a variant with no `_` arm -> non-exhaustiveness warning.
enum Color:
    Red
    Green
    Blue

def name_len(c: Color) -> int64:
    match c:
        case Red:
            return 3
        case Green:
            return 5
