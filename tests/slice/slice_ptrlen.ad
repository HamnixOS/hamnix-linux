# Slice construction from an explicit (ptr, len) pair.
def main(argc: int32, argv: Ptr[uint64]) -> int32:
    a: Array[8, int32]
    a[5] = 42
    p: Ptr[int32] = cast[Ptr[int32]](a)
    s: Slice[int32] = Slice[int32](p, 8)
    return s[5]
