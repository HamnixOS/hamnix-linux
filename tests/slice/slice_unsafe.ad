# unsafe: suppresses the slice bounds check -> no trap (exit 0-ish).
def main(argc: int32, argv: Ptr[uint64]) -> int32:
    a: Array[4, int32]
    s: Slice[int32] = Slice[int32](a)
    r: int32 = 0
    unsafe:
        i: int32 = argc - 1
        r = s[i]
    return 0
