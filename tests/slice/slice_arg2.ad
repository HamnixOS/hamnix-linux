# 2-arg Slice construction from a param pointer (no local array -> no canary).
def main(argc: int32, argv: Ptr[uint64]) -> int32:
    s: Slice[uint64] = Slice[uint64](argv, cast[uint64](argc))
    return cast[int32](s.len) + cast[int32](s[0])
