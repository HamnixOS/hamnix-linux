# The unsupported form: a sub-slice used directly as a subexpression.
def main(argc: int32, argv: Ptr[uint64]) -> int32:
    a: Array[4, int32]
    a[0] = 7
    s: Slice[int32] = Slice[int32](a)
    return s[1:3][0]        # bare inline sub-slice -> clean compile error
