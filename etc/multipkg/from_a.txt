SCRATCH_A_PAYLOAD: this file was installed onto the target ext4
by /etc/install_multipkg.hamsh via install_file_to_slot, simulating
the file payload of a synthetic "scratch-package-a" hpm package.
If you see this AND from_b.txt after rebooting from the installed
disk alone, the multi-package install path works: the second
install_file_to_slot call did not clobber the first.
