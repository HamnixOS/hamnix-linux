SCRATCH_B_PAYLOAD: this file came from a separate synthetic
"scratch-package-b" hpm package, written onto the target ext4
after from_a.txt by /etc/install_multipkg.hamsh. The fact that
from_a.txt is ALSO present after a reboot is the multi-package
install proof — see scripts/test_install_multipkg.sh.
