# /etc/rc.d/rc.6 — runlevel 6 (reboot) operator hook.
#
# Sourced by PID-1 hamsh's svc_enter_runlevel(6) BEFORE the built-in
# stop-all + reboot action (which writes "reboot" to /dev/reboot). Put
# any pre-reboot work here. Do NOT call `init`/`svc runlevel` from this
# file (it would recurse / no-op). Edit freely.
echo '[rc.6] runlevel 6 hook: preparing for reboot'
